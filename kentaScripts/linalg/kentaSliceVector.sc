import breeze._
import breeze.linalg._

/**
 * @author ktakagaki
 * @date 2/2/14.
 */
val tempDV = DenseVector(0, 1, 2, 3, 4, 5, 6)
tempDV( 0 ## 3 )
tempDV( 4 ## -1 )
tempDV( -3 ## -1 )
tempDV( 0 ## 4 ## 2 )
tempDV( 4 ## 0 ## -2 )

